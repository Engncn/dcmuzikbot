const { client } = require('../index.js');
const chalk = require("chalk");
const { Guild } = require('discord.js');
let array =
client.on("ready", async () => {

    console.log(chalk.cyan(`============================================`));
    console.log(chalk.cyan(`||              ✰ Youiss#0001             ||`));
    console.log(chalk.cyan(`||       YouMusic v12        ||`));
    console.log(chalk.cyan(`============================================`));
    console.log(chalk.cyan(`===========311184407115333643===================`));
   

    await client.user.setActivity("SAFE CODE / YOUİSS", {type: "LISTENING"})


});